apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# Both files are applied directly by assert.sh; one expects
# acceptance, the other rejection. Listing them here makes the
# scenario kustomize-applyable for inspection.
resources:
- accepted.yaml
