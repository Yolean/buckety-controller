apiVersion: buckety.yolean.se/v1alpha1
kind: Buckety
metadata:
  name: orders
spec:
  backend: s3
  retentionPolicy: Delete
  defaultAccess:
    role: ReadWrite
    credentialsSecretName: orders-bucket
