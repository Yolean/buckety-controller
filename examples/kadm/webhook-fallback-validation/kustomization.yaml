apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# The invalid resources are applied by assert.sh while the
# webhook is disconnected; only the valid Buckety goes in upfront.
resources:
- base.yaml
