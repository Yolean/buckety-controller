# yaml-language-server: $schema=https://raw.githubusercontent.com/Yolean/buckety-controller/main/schema/buckety-kadm.schema.json
# Collides with the pre-created, content-holding resource; the
# default adoption policy (AdoptEmpty) must refuse it.
apiVersion: buckety.yolean.se/v1alpha1
kind: Buckety
metadata:
  name: adopt-pre
spec:
  backend: kafka
  retentionPolicy: Delete
  defaultAccess:
    role: ReadWrite
    credentialsSecretName: adopt-pre-creds
