apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# The consumer Job is applied separately by assert.sh after the
# operator is scaled to zero; only the Buckety is provisioned
# upfront here.
resources:
- buckety.yaml
