apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# This scenario operates on the controller's own config Secret;
# the harness handles apply/restore. Nothing namespace-scoped
# is applied via kustomize here.
resources: []
